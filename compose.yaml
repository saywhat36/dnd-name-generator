services:
  postgres:
    image: 'postgres:16-alpine'
    environment:
      - 'POSTGRES_DB=namegen'
      - 'POSTGRES_PASSWORD=namegen'
      - 'POSTGRES_USER=namegen'
    ports:
      - '5432:5432'
